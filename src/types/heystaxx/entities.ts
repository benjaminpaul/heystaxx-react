export interface Industry {
    id: string;
    name: string;
}

export interface Country {
    id: string;
    name: string;
    countryCode: string;
}

export interface Timezone {
    id: string;
    name: string;
}

export interface Jobtitle {
    id: string;
    title: string;
}